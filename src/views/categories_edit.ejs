<% include partials/_header %>

<div class="container mt-5">
    <div class="row">
        <div class="col-md-5">
            <div class="card form">
                <div class="card-body">
                    <form action="/update-category/<%= data.id %>" method="POST">
                        <div class="form-group">
                            <input class="form-control m-1" type="text" name="name" value="<%= data.category_name %>">
                        </div>
                        <button type="submit" class="btn btn-primary m-1">Edit category</button>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>


<% include partials/_footer %>