<% include partials/_header %>

<div class="container">
    <div class="row mt-5">
        <div class="col-md-7">
            <div class="table-responsive">
                
            <table class="table table-bordered table-hover">
                    <thead>
                        <th>N°</th>
                        <th>Name</th>
                        <th>Actions</th>
                    </thead>
                    <tbody>
                        <% if(data){ %>
                            <% for(var i = 0; i < data.length; i++){ %>
                                <tr>
                                    <td><%= (i + 1) %></td>
                                    <td><%= data[i].category_name %></td>
                                    <td>
                                        <center><a href="/delete-category/<%= data[i].id %>"><button class="btn btn-danger">Delete</button></a>
                                        <a href="/update-category-panel/<%= data[i].id %>"><button class="btn btn-info mt-1">Update</button></a></center>
                                    </td>
                                </tr>
                            <% } %>
                        <% } %>
                    </tbody>
                </table>
            </div>
        </div>
        <div class="col-md-5">
            <div class="card form">
                <div class="card-body">
                    <form action="/add-category" method="POST">
                        <div class="form-group">
                            <input class="form-control m-1" type="text" name="name" placeholder="Category name">
                        </div>
                        <button type="submit" class="btn btn-primary m-1">Save category</button>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

<% include partials/_footer %>